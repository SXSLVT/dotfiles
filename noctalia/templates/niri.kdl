layout {
    focus-ring {
        active-color "{{colors.primary.default.hex}}"
        inactive-color "transparent"
    }

    border {
        active-color "{{colors.primary.default.hex}}"
        inactive-color "transparent"
    }
}
